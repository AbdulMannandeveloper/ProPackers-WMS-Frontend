# Development: Vite serves on :5173, the API on :8000, so requests cross.
# Production serves both from one origin through nginx — set this to an
# empty string there so requests go to /api on the same host.
VITE_BASE_API_URL=http://localhost:8000
